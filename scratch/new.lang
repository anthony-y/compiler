type Whatever: ^^^bool;
type Fun: struct {
    big_number: u64;
    string_ptr: ^string;
}

proc test(): Whatever {
    z := true;
    w : ^bool = ^z;
    return ^^w;
}

proc have_fun(): Fun {
    f: Fun;
    f.big_number = 10
    f.string_ptr = 10
}

proc main() {
    //boolean : bool = value; // bad error message
    val : Whatever = test()

    //value: struct{n: u64; s: string} = #place have_fun()
}
