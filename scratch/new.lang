type Whatever: ^^^bool;
type Fun: struct {
    big_number: u64;
    string_ptr: ^string;
}

proc test(): Whatever {
    z := true;
    w : ^bool = ^z;
    return ^^w;
}

type Alias2: ^^^bool;
proc value(i: Alias2) {

}

type S: struct {
    whatever: string;
    es: struct {
        s: u32
    }
}

type V: int;

i : V;

proc main(): V {
    val : Whatever = test()
    //boolean : bool = value; // bad error message
    a := 10
    b := a;
    value(null);
    return 10;
}
