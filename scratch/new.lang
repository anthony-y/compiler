type Fun: struct {
    big_number: u64;
    string_ptr: ^string;
}

proc test(): Whatever {
    z := true;
    w : ^bool = ^z;
    return ^^w;
}

type Whatever: ^^^bool;
type Alias2: ^^^bool;
proc value(i: Alias2) {

}

proc main() {
    val : Whatever = test()
    //boolean : bool = value; // bad error message
    b := false;
    value(null);
    return;
}
