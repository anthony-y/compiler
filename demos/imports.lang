#import "std/mem.lang"
#import "std/basic.lang"

msg := "Arena allocator from another module\n";
len := msg.length;

proc main() {
	arena: Arena;
	defer arena_free(^arena);

	bytes := arena_alloc(^arena, len);
	mem_copy(bytes, msg.data, len);
	bytes[len] = 0;

	print("%s".data, bytes);
}
